# Clouvent

Next.js application for Clouvent. See `CLAUDE.md`, `PROJECT_BRIEF.md`, and `BRAND_SYSTEM.md` in the project root for the governing development constitution, business source of truth, and brand implementation source of truth.

## Development

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run build
```
